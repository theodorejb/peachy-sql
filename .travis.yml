language: php

php:
  - 5.5
  - 5.6
  - 7.0
  - hhvm
  - hhvm-nightly

install:
  - composer install

before_script:
  - mysql -e 'create database PeachySQL;'

script: phpunit

matrix:
  allow_failures:
    - php: 7.0
    - php: hhvm-nightly
